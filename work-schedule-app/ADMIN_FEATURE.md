# 👑 Admin Функционалност - Документация

## ✅ Преглед

Добавена е admin функционалност която позволява на специални потребители да:
- ✅ Добавят нови служители в системата
- ✅ Премахват съществуващи служители (soft delete)

---

## 👥 Admin Потребители

Само следните двама потребителя имат admin права:
- **Юлия Любенова**
- **Борис Иванов**

---

## 🔐 Как работи

### Admin проверка

```javascript
// В AuthContext.jsx
const isAdmin = user?.name === 'Юлия Любенова' || user?.name === 'Борис Иванов';
```

**Логика:**
- Проверява се името на текущия потребител
- Ако е един от двамата admin потребители → `isAdmin = true`
- Иначе → `isAdmin = false`

---

## 📱 UI Елементи

### 1. Admin бутон в Home Page

**Локация:** Най-вдясно в status-info бара (до "Вкъщи" бутона)

**Дизайн:**
- 👑 Crown икона
- 🟠 Оранжев gradient (#f59e0b → #d97706)
- Hover ефект (lift up + shadow)

**Видимост:**
- ✅ Видим само на **Desktop** (> 768px)
- ✅ Видим само за **admin потребители**
- ❌ Скрит на mobile устройства

**Код:**
```jsx
{isAdmin && isDesktop && (
    <button onClick={() => setCurrentPage('admin')}>
        <i className="fas fa-crown"></i>
        Администрация
    </button>
)}
```

---

### 2. Admin страница

**Route:** `/admin` (или `currentPage === 'admin'`)

**Компонент:** `src/pages/Admin.jsx`

**Функции:**
1. **Добавяне на служител** - форма с име, място, група
2. **Преглед на всички служители** - списък с всички (активни и неактивни)
3. **Премахване на служител** - soft delete (is_active = false)

---

## 🎯 Функционалности

### Добавяне на служител

**Форма:**
- ✅ Име на служителя (задължително)
- ✅ Номер на място (опционално)
- ✅ Група на място (1, 2 или 3)

**Валидация:**
- Името е задължително
- Останалите полета са опционални

**Database операция:**
```sql
INSERT INTO employees (name, seat_number, seat_group, is_active)
VALUES ('Иван Петров', '348', 1, true);
```

---

### Премахване на служител

**Процес:**
1. Admin кликва на 🗑️ иконата
2. Появява се confirmation dialog
3. При потвърждение → `is_active = false`
4. Служителят **НЕ се изтрива** от базата (soft delete)
5. Служителят вече **НЕ се показва** в Login списъка

**Database операция:**
```sql
UPDATE employees 
SET is_active = false 
WHERE id = employee_id;
```

---

## 📊 UI Дизайн

### Admin страница

```
┌─────────────────────────────────────┐
│  👑 Администрация  [+ Добави]     │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │ ➕ Добавяне на нов служител │   │
│  │                             │   │
│  │ Име: [____________]         │   │
│  │ Място: [___] Група: [1▼]    │   │
│  │                             │   │
│  │ [✨ Добави служител]        │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 👥 Служители (24 общо)     │   │
│  │                             │   │
│  │ ├─ София Григорова    🗑️   │   │
│  │ │  Място 348 • Група 1     │   │
│  │                             │   │
│  │ ├─ Борис Иванов       🗑️   │   │
│  │ │  Място 353 • Група 2     │   │
│  │                             │   │
│  │ ├─ [Неактивен] Иванов      │   │
│  │ │  Място 350 • Група 1     │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

## 🔒 Security

### Frontend защита:
- ✅ Admin проверка преди показване на бутон
- ✅ Admin проверка в Admin компонента
- ✅ Ако не е admin → показва "Нямате достъп"

### Backend защита (Supabase RLS):
- ⚠️ **ВАЖНО:** Трябва да добавиш RLS политики!

**Препоръчително:**
```sql
-- Само admin могат да добавят служители
CREATE POLICY "Only admins can insert employees"
ON employees FOR INSERT
TO authenticated
WITH CHECK (
    -- Проверка дали е admin (може да се направи с role колона)
    true  -- За сега позволяваме, но в production трябва проверка!
);

-- Само admin могат да обновяват is_active
CREATE POLICY "Only admins can update employees"
ON employees FOR UPDATE
TO authenticated
USING (true)
WITH CHECK (true);
```

**Забележка:** Тъй като сега използваш само име за проверка, RLS не може да разбере дали е admin. За production препоръчвам:
1. Добави `role` колона в `employees` таблицата
2. Или използвай Supabase Auth с custom claims

---

## 💻 Код Структура

### Файлове:

```
src/
├── pages/
│   └── Admin.jsx              # Admin страница
├── contexts/
│   └── AuthContext.jsx         # Добавен isAdmin check
└── App.jsx                     # Добавен Admin route
```

### State Management:

```javascript
// AuthContext
const isAdmin = user?.name === 'Юлия Любенова' || user?.name === 'Борис Иванов';

// Admin.jsx
const [employees, setEmployees] = useState([]);
const [showAddForm, setShowAddForm] = useState(false);
const [newEmployee, setNewEmployee] = useState({ name: '', seat_number: '', seat_group: 1 });
```

---

## 🎨 Стилове

### Admin бутон:
- **Цвят:** Оранжев gradient (#f59e0b → #d97706)
- **Икона:** Crown (👑)
- **Hover:** Lift up + увеличен shadow
- **Responsive:** Скрит на mobile

### Admin страница:
- **Форма:** Същия стил като Events форма
- **Списък:** Същия стил като Events списък
- **Неактивни служители:** Синьо-червен border, по-ниска opacity

---

## 📱 Responsive

### Desktop (> 768px):
- ✅ Admin бутон е видим в status-info bar
- ✅ Admin страница е пълнофункционална

### Mobile (≤ 768px):
- ❌ Admin бутон е скрит
- ✅ Admin страница все още работи (ако се достигне директно)

**Забележка:** На mobile можеш да добавиш admin бутон в Sidebar или BottomNav ако е необходимо.

---

## 🧪 Тестване

### Тест 1: Admin потребител
```
1. Влез като "Юлия Любенова" или "Борис Иванов"
2. Отиди на Home страницата
3. Виждаш оранжев бутон "👑 Администрация"
4. Кликни на него
5. Виждаш Admin страницата
```

### Тест 2: Не-admin потребител
```
1. Влез като друг потребител
2. Отиди на Home страницата
3. НЕ виждаш admin бутон ✅
4. Ако се опиташ да отвориш /admin → "Нямате достъп"
```

### Тест 3: Добавяне на служител
```
1. Admin → Отвори Admin страница
2. Кликни "Добави служител"
3. Попълни формата
4. Кликни "Добави служител"
5. Виждаш success съобщение
6. Новият служител се появява в списъка
7. Може да се логне в системата ✅
```

### Тест 4: Премахване на служител
```
1. Admin → Отвори Admin страница
2. Намери служител в списъка
3. Кликни на 🗑️ иконата
4. Потвърди
5. Виждаш success съобщение
6. Служителят се маркира като "Неактивен"
7. Вече не може да се логне ✅
```

---

## 🔮 Бъдещи подобрения

### Възможни разширения:
- 📝 Редактиране на съществуващи служители
- 🔄 Reactivate на неактивни служители
- 📊 Статистика за служители
- 📧 Email notifications при добавяне/премахване
- 🔐 По-сигурна admin проверка (с role колона)
- 📱 Mobile admin достъп (ако е необходимо)

---

## ⚠️ Важни бележки

### 1. Security
- За сега admin проверката е само в frontend
- За production трябва да добавиш backend проверка
- Препоръчително: добави `role` колона в `employees` таблицата

### 2. Soft Delete
- Служителите не се изтриват напълно
- `is_active = false` ги скрива от Login списъка
- Все още се виждат в Admin страницата (за transparency)

### 3. Mobile
- Admin бутонът е скрит на mobile
- Admin страницата все още работи ако се достигне директно
- Можеш да добавиш в Sidebar/BottomNav ако е необходимо

---

## 🚀 Готово!

Admin функционалността е напълно интегрирана и готова за употреба!

**Основни предимства:**
- ✅ Лесно добавяне на нови служители
- ✅ Безопасно премахване (soft delete)
- ✅ Само за специални потребители
- ✅ Видим само на Desktop
- ✅ Красив UI

Enjoy! 🎉

